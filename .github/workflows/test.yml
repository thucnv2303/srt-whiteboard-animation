name: Test app

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Compile Python
        run: python -m py_compile whiteboard_app/*.py scripts/*.py run_app.py
      - name: Run unit tests
        run: python -m unittest discover -s tests -v

